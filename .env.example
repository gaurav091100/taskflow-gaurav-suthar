# Ports published to your machine (docker compose)
MOCK_API_PORT=4000
WEB_PORT=3000

# Local dev (Vite): optional; defaults to http://localhost:4000 in src/config.ts
# When using MSW only, this should still match MSW handler URLs.
VITE_API_BASE_URL=http://localhost:4000
